{{ form_start(form) }}
  {{ form_row(form.name) }}
  {{ form_row(form.description) }}
  <div class="mt-3">
    <button class="btn btn-primary">💾 Enregistrer</button>
    <a class="btn btn-secondary" href="{{ path('phone_category_index') }}">↩︎ Annuler</a>
  </div>
{{ form_end(form) }}
